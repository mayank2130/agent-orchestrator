services:
  web:
    image: ao-preview-${BRANCH_SLUG:-pr}
    build:
      context: ..
      dockerfile: preview/Dockerfile
    ports:
      - "${PORT:-3500}:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production
